#include <stdio.h>

/**main - prints the name of the programme
 *
 * @argc: number of command line
 *
 * @argv: array name
 *
 * Return: success 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
